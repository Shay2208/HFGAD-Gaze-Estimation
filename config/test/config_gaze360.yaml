test:

    load:
        begin_step: 65
        end_step: 80
        steps: 10

    data: 
        # 修改为本地数据集路径，目录结构参考 README "数据集准备" 一节
        image: "./datasets/Gaze360/Image"
        label: "./datasets/Gaze360/Label/test.label"
        name: gaze360
        header: True
        isFolder: False

    savename: "evaluation"

    device: 0

    reader: reader

# dropout = 0
# dim_feed = 512
